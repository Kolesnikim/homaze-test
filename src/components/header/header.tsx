import React from "react";
import './header.scss';

function HmHeader(): JSX.Element {
    return (
        <header className="hm-header">
            <h1 className="hm-header__title">
                Contracts
            </h1>
        </header>
    )
}

export default HmHeader;